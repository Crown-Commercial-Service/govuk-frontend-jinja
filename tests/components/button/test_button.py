def test_button_default(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_disabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_link(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_link_disabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_start(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_start_link(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_input(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_input_disabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_prevent_double_click(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_with_active_state(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_with_hover_state(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_with_focus_state(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_secondary(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_secondary_disabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_secondary_link(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_warning(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_warning_disabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_warning_link(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_inverse(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_inverse_disabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_inverse_link(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_inverse_start_button(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_attributes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_link_attributes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_input_attributes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_classes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_link_classes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_input_classes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_name(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_type(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_input_type(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_explicit_link(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_no_href(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_value(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_html(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_no_type(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_no_data_prevent_double_click(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_dont_prevent_double_click(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_button_id(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


