def test_input_default(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_hint_text(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_error_message(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_width_2_class(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_width_3_class(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_width_4_class(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_width_5_class(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_width_10_class(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_width_20_class(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_width_30_class(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_label_as_page_heading(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_optional_form_group_classes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_autocomplete_attribute(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_pattern_attribute(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_spellcheck_enabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_spellcheck_disabled(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_suffix(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_and_suffix(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_and_long_suffix(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_and_suffix_and_error(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_and_suffix_and_width_modifier(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_extra_letter_spacing(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_classes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_custom_type(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_value(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_describedby(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_attributes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_hint_with_describedby(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_error_with_describedby(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_error_and_hint(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_error_hint_and_describedby(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_inputmode(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_with_html_as_text(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_with_html(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_with_classes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_prefix_with_attributes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_suffix_with_html_as_text(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_suffix_with_html(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_suffix_with_classes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


def test_input_with_suffix_with_attributes(env, similar, template, expected):
    template = env.from_string(template)
    assert similar(template.render(), expected)


